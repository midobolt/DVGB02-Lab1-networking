# DVGB02-Lab1-networking

Time:

1.Build the programs Compile the programs using gcc:

  gcc server.c -o server 
  gcc client.c -o client

2.Run the server Start the server in one terminal
  ./server

3.Run the client Start the client in another terminal   
  ./client
